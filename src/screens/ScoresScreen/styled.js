import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  result: {
    // display: "flex",
    alignItems: "center",
  },
  isCorrect: {
    marginRight: 10
  },
  buttonContainer: {
    marginTop: 20,
  }
})
